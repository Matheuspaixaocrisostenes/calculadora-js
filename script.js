function botao( num ){
    var salvo = document.calc.visor.value
    document.calc.visor.value = salvo + num
}

function reset(){
    document.calc.visor.value = ""
}

function calcule(){
    var salvo = document.calc.visor.value
    eval
}